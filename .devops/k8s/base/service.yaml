apiVersion: v1
kind: Service
metadata:
  name: medisynth-api-svc
  namespace: medisynth-io
  labels:
    app: medisynth-api
spec:
  type: ClusterIP
  ports:
  - port: 80 
    targetPort: 8081 
    protocol: TCP
    name: http
  selector:
    app: medisynth-api